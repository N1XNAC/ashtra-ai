fastapi==0.116.1
uvicorn==0.35.0
sqlalchemy==2.0.43
pydantic==2.11.7
pydantic-settings==2.10.1
# python-jose[cryptography]==3.5.0  # requires cryptography compilation — add after deploy
# passlib[bcrypt]==1.7.4  # requires bcrypt compilation — add after deploy
python-multipart==0.0.20
httpx==0.28.1
sse-starlette==3.0.2
# numpy  # not needed at startup, local_model is lazy-imported
psycopg2-binary  # add when DATABASE_URL is postgresql://
# optional, upgrades semantics when installed:
# sentence-transformers==3.3.1
